# Server-side secrets. DO NOT COMMIT. Copy to .env and fill in locally.
# On Vercel: Project Settings → Environment Variables.

# Password for the superadmin login (username left blank). The
# superadmin can create/suspend/delete individual admin accounts from
# the "Admins" panel in the dashboard — that's the only way to get a
# regular admin login working, there's no more shared admin password.
SUPERADMIN_PASSWORD=change-me-to-a-strong-password

# 32+ char random string used to sign admin session cookies.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
ADMIN_SESSION_SECRET=replace-with-a-long-random-string
